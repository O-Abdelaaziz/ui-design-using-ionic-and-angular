import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '21-ecommerce-welcome-login-screen-app',
  webDir: 'www'
};

export default config;
