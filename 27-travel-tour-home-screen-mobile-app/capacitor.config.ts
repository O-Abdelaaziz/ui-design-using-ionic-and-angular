import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '27-travel-tour-home-screen-mobile-app',
  webDir: 'www'
};

export default config;
