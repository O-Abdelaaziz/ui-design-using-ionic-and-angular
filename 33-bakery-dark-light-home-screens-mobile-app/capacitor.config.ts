import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '33-bakery-dark-light-home-screens-mobile-app',
  webDir: 'www'
};

export default config;
