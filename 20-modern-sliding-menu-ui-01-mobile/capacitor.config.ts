import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '20-modern-sliding-menu-ui-01-mobile',
  webDir: 'www'
};

export default config;
