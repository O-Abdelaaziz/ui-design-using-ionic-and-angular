import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '24-ecommerce-welcome-login-signup-otp-screen-app',
  webDir: 'www'
};

export default config;
