import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '16-car-rental-home-page-screen-mobile-app',
  webDir: 'www'
};

export default config;
