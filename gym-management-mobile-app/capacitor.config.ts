import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gym-management-mobile-app',
  webDir: 'www'
};

export default config;
