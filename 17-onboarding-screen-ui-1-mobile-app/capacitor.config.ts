import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '17-onboarding-screen-ui-1-mobile-app',
  webDir: 'www'
};

export default config;
