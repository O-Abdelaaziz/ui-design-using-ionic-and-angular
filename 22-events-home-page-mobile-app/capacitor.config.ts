import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '22-events-home-page-mobile-app',
  webDir: 'www'
};

export default config;
