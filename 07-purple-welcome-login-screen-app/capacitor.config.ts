import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '07-purple-welcome-login-screen-app',
  webDir: 'www'
};

export default config;
