import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '13-flat-purple-welcome-login-screen-app',
  webDir: 'www'
};

export default config;
