import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '26-instagram-clone-home-screens-mobile-app',
  webDir: 'www'
};

export default config;
