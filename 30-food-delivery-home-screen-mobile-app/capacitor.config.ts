import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '30-food-delivery-home-screen-mobile-app',
  webDir: 'www'
};

export default config;
