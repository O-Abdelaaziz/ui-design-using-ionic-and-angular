import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '32-events-hub-home-screens-mobile-app',
  webDir: 'www'
};

export default config;
