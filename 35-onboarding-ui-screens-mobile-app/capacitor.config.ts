import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '35-onboarding-ui-screens-mobile-app',
  webDir: 'www'
};

export default config;
