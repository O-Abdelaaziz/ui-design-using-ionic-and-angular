import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '31-gift-store-home-screens-mobile-app',
  webDir: 'www'
};

export default config;
