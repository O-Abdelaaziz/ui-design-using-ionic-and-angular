import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '34-home-automation-ui-desgin-mobile-app',
  webDir: 'www'
};

export default config;
