import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '28-dynamic-error-screens-mobile-app',
  webDir: 'www'
};

export default config;
