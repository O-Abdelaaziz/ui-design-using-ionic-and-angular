import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '12-covid-home-page-mobile-app',
  webDir: 'www'
};

export default config;
