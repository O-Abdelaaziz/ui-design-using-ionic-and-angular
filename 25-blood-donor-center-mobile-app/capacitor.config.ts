import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '25-blood-donor-center-mobile-app',
  webDir: 'www'
};

export default config;
