import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '10-fashionista-welcome-login-screen-app',
  webDir: 'www'
};

export default config;
