import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '09-elearning-welcome-login-screen-app',
  webDir: 'www'
};

export default config;
