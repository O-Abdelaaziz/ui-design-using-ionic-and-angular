import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '29-easy-pay-wallet-finance-home-screen-mobile-app',
  webDir: 'www'
};

export default config;
